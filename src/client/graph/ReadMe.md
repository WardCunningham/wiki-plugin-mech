Graph
=====
We duplicate the source code for graph here from the original repo. [GitHub](https://github.com/WardCunningham/graph)

This replaces a less formal borrowing in ../library.js

We delegate the Mech's api.graph(nodes,rels) to the Graph constructor in that block.
